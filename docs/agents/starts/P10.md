# P10 Start Notes

Your issue owns cached Compiler candidate extraction. The approved test interface is the highest Core candidate-extraction use case, using actual source bytes, parser, chunker and cache with a fake ModelProvider. Public compile execution, Proposal persistence and durable Run recovery belong to later tickets.

Read the ticket and its SPEC references, then inspect:

- Core `ingest/cache` and `StageKey::CandidateExtract`, plus `parse_cached` and `chunk_cached`.
- Core `model::generate`, `GenerationOptions`, ModelProvider and request/output contracts.
- Entity advice as prior art for retaining usage after semantic validation failure.
- SourceLibrary content verification and effective Schema loading.
- Existing `cached_ingest`, `model_generation`, `stage_cache`, `source_parse`, `chunking` and `entity_advice` tests.

Bind mode, focus, language and actual sent input into the candidate identity. `refresh` is extraction context here; comparison against historical accepted assertions belongs to P18. Define bounds and a deterministic strategy for combining chunk-local temporary IDs before validating the aggregate.

Each call to existing `model::generate` has its own budget. A multi-chunk extraction must account for cumulative usage/calls instead of giving every chunk the entire operation allowance. Retain usage when structured output passes but candidate semantic validation fails. Durable pre-request admission remains P14; keep the extraction integration compatible with that future hook.

Add the smallest highest-interface candidate integration test target appropriate to the module. Existing regression command:

```bash
cargo test -p knowmesh-core --locked --test cached_ingest --test model_generation --test stage_cache --test source_parse --test chunking --test entity_advice --test architecture
```

Compiler candidate DTOs, the versioned Compiler prompt and candidate cache integration belong to this task. No temporary public CLI, new runtime tables, actual model-service credentials or canonical write authority is needed. Preserve current parser/model callers. Report any required shared-contract change before making another branch depend on it.
