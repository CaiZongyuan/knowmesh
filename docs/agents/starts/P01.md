# P01 Start Notes

Your issue owns the material inventory and reproducible import/lexical baseline. The approved verification interface is existing CLI processes in a temporary workspace plus checks of the produced inventory/report. No production Rust change or model call is needed.

Start from SPEC 2.3 and 22.4-22.6. Cover the named research subjects: STATE, scGPT, Geneformer, Tahoe-100M, Perturb-seq and Virtual Cell Challenge. Prefer primary-source records; mark inaccessible originals or uncertain redistribution rights explicitly. Existing local material findings in `docs/configuration-checks.md` are leads to verify, not an authoritative gold dataset.

Create one owning inventory/baseline location under `evals/`, which is not yet present at the baseline. Keep independently acquired originals out of distributable fixtures when redistribution is not established. Include acquisition references and content hashes so a later evaluator can reproduce the input.

Current Source FTS indexes title, authors and tags. Parsed body Chunk indexing belongs to P09. Establish successful metadata searches and honestly record body-query gaps; do not implement P09 to make this baseline appear stronger.

Use stable inventory IDs and content hashes across fresh workspaces. Source and Revision ULIDs are allocated identities and need not match between fresh runs. To verify repeat import in one workspace, reuse the recorded `--source-id`; calling source add without it allocates another Source.

Inspect existing Source and Search CLI fixtures and shared temporary-workspace support. Regression command:

```bash
cargo test -p knowmesh --locked --test source_cli --test source_read_cli --test search_cli
```

The delivered baseline itself executes version, init, source add, sync, source get/content and English/Chinese search. Record commands, material hashes, actual results and acquisition failures. Prepare at least 20 excerpt and 10 question candidates with an explicit unreviewed state; neither an agent nor a model signs the human gold/quality judgment.

Keep new data, scripts and documentation scoped to this baseline. Use the documentation workflow; do not invoke a workflow that needs additional agent slots merely to summarize available material. If a baseline script changes, test its actual successful and failed acquisition/import paths. Hand off the inventory and report without beginning downstream Compiler or Answer evaluation.
